// classes
import { Input } from './Input';
import { onChange, onPaste, onFocus, onBlur, onError } from './Event';


export const event = {
    onChange,
    onPaste,
    onFocus,
    onBlur,
    onError,
};
export {
    Input,
};